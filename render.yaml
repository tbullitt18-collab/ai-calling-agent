services:
  - type: web
    name: ai-calling-agent-mcp
    runtime: python
    plan: starter
    buildCommand: bash install_deps.sh
    startCommand: python mcp_server.py
    envVars:
      - key: PORT
        value: 8000
